[build]
builder = "nixpacks"
buildCommand = "cd baths-engine/backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd baths-engine/backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
